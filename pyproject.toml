[project]
name = "pricepulse"
version = "0.1.0"
dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "psycopg2-binary",
    "pydantic",
    "pydantic-settings",
    "email-validator"
]

[tool.vercel.python]
maxLambdaSize = "15mb"

[tool.vercel.entrypoint]
file = "api/main.py"
